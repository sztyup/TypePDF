export enum ViewerPreferenceEnforce {
    /**
     * This name may appear in the Enforce array only if the corresponding
     * entry in the viewer preferences dictionary specifies a valid value other than AppDefault
     */
    PrintScaling = 'PrintScaling',
}
